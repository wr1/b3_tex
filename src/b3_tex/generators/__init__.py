"""Fabric generators: data -> ``tuple[ParametricYarn]`` consumed by a weave field.

Each generator turns a fabric specification (pattern + geometry) into yarns; the
``fabric_registry`` wires them to YAML ``field.type`` strings.
"""
